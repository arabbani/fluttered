import 'package:flutter/material.dart';

var defaultTheme = 'theme1';

var availableThemes = {
  'theme1': ThemeData(primarySwatch: Colors.brown, primaryColor: Colors.brown),
  'theme2': ThemeData(primarySwatch: Colors.red, primaryColor: Colors.red),
};
